# AGENTS.md

本文件定义酒店运营系统项目中所有开发代理必须遵守的规则。开始任何任务前，必须先阅读本文件，再阅读相关文档和现有代码。

## 1. 必读文档

按以下优先级阅读：

1. `docs/PRD.md`：产品需求、业务规则、验收标准和待核实事项的权威来源。
2. `prototype/README.md`：UI 原型方向和原型边界。
3. `README.md`：当前仓库和历史演示代码说明。
4. `prototype/hotel-prototype.html`：已确认的视觉方向参考，不是生产代码。

如果代码与 `docs/PRD.md` 冲突，以 PRD 为准，不要反向修改 PRD 来迁就代码。

## 2. 当前项目状态

- 当前处于 PRD 和原型确认阶段，正式开发尚未开始。
- `docs/PRD.md` 是业务规则的权威来源。
- `prototype/` 是一次性 UI 原型，仅用于确认信息架构和视觉方向。
- `public/`、`server.mjs`、`package.json` 是早期零依赖演示 MVP，不是正式产品架构。
- 不得将原型代码或演示 MVP 直接提升为生产实现。
- 未经用户明确要求，不要开始搭建正式项目、数据库、接口或部署环境。

## 3. 已确认产品边界

### 3.1 产品定位

- 单体酒店内部使用的桌面 Web 系统。
- 第一阶段不实现多租户、SaaS 授权、集团管理。
- 代码结构可以为未来产品化留出边界，但不得提前实现产品化能力。

### 3.2 酒店事实

- 客房总数：40 间。
- 楼层：7、8、9、14 层。
- 每层：10 间。
- 前台电脑：1 台。
- 第一版并发设计：1 名用户，但后端仍需事务保护。
- 班次：白班 08:00～17:00；夜班 17:00～次日 08:00。
- 第一版渠道：到店、电话、微信。
- 第一版不经营和对接 OTA。
- 平均房价：100 元。
- 仅桌面端，不实现移动端。

### 3.3 第一版范围

- 基础资料、房型、房间、日期房价和税率。
- 直订预订、排房、入住、续住、提前退房、换房、退房。
- 占用状态、清洁状态和可用状态。
- 退房清洁、清洁完成和查房。
- 押金、房费、消费、收款、退款、挂账和余额。
- 班次交班、差异确认、日报和营业日关闭。
- 用户、权限、审计、备份和恢复。

### 3.4 明确禁止扩展

除非 PRD 先更新并获得用户确认，否则不得实现：

- 移动端或清洁人员手机端。
- OTA、渠道管理器或渠道 API。
- 多酒店、多租户或集团功能。
- 公安系统、身份证读卡器、证件照片或门锁。
- 税控发票。
- 会员积分、等级、储值或营销。
- 餐饮、SPA、会议室、库存和采购。
- 团队主单、联房和统一结算。
- 钟点房、日用房和按小时计费。
- 动态定价、复杂促销和自动夜审。
- 人脸识别和机器人服务。

## 4. 不可违反的业务规则

1. 一个订单只能对应一间房。
2. 确认预订前必须指定具体房间。
3. 同一房间的日期范围不得重叠。
4. 第一版禁止超售。
5. 日期范围统一使用 `[checkIn, checkOut)`，离店日不占用房间。
6. 订单、账务、退款和审计日志不得物理删除。
7. 已入住订单不能直接取消，必须先完成退房。
8. 房态必须拆分，不能合并为单一字段：
   - 占用状态：空闲、已锁定、在住
   - 清洁状态：待清洁、已清洁、已查房
   - 可用状态：可正常使用、停用维修
9. 只有同时满足“已查房 + 可正常使用 + 未被有效订单占用”的房间才能出售。
10. 退款、改价、挂账、差异确认和例外处理必须由经理重新认证。
11. 已关闭营业日的账务修正必须记录审批人和修改原因。
12. 交接班存在差异时必须填写原因，不能静默清零。
13. 未到店费用只能由经理审批后手工记账，不做自动收费。
14. 票价含税，日报同时记录含税金额、不含税金额和税额。
15. 员工账号不得共用；每个自然人员工使用独立账号。

## 5. 技术架构约束

正式开发开始时，目标是以下技术方案：

- 前端：Vue 3 + TypeScript + Vite。
- 后端：Java + Spring Boot + Spring Security。
- 数据访问：MyBatis-Plus 或 Spring Data JPA，最终选择尚未确定。
- 数据库：MySQL 8。
- 部署：本地独立服务器 + UPS，局域网浏览器访问。
- 备份：MySQL 备份，加密写入外置硬盘和云对象存储。
- 时区：`Asia/Shanghai`。
- 浏览器：当前版本 Chrome 和 Edge。
- 服务端必须执行权限校验，前端隐藏按钮不构成安全措施。

在技术选择尚未写入 PRD 时，不要自行锁定 Java 版本、ORM、云厂商、目录结构或认证实现。先更新 PRD 或向用户确认。

## 6. 数据与安全规则

- 金额在 Java 中使用 `BigDecimal`，在数据库中必须使用 `DECIMAL`，禁止使用浮点数。
- 手机号和证件号码必须加密存储。
- 证件号码使用不可逆哈希进行查重。
- 界面只显示脱敏证件号码。
- 不保存证件照片。
- 密码使用 BCrypt 或 Argon2id 哈希。
- 数据库备份与加密密钥必须分开保存。
- 局域网站点必须使用 HTTPS。
- 经理远程访问只能通过 VPN。
- 审计日志普通用户不可删除，第一版至少保留 2 年。
- 任何测试、截图或示例数据必须使用虚构数据，不得包含真实宾客隐私。

## 7. 原型与界面方向

已确认采用混合方案：

- 以原型 A“值班控制台”为主框架。
- 吸收原型 B 的今日时间线和流程表达。
- 吸收原型 C 的右侧详情面板。
- 房态矩阵是运营首页的核心视觉区域。
- 不使用纯看板作为主导航。
- 不使用全表格台账作为首页。
- 第一版只设计桌面端。

生产实现不得包含原型底部的方案切换器、`PROTOTYPE` 标签或原型交互占位弹窗。

## 8. 开发流程

开始编码前：

1. 阅读 `docs/PRD.md`。
2. 检查 PRD 第 15 章的待核实事项是否影响当前任务。
3. 如果任务要求改变业务规则，先更新 PRD 并获得用户确认。
4. 如果需求存在歧义，停止实现并明确询问，不得自行猜测并扩展范围。
5. 保持改动聚焦，不顺手重构无关模块。

任务完成后必须说明：

- 修改了哪些文件。
- 实现了哪些 PRD 编号。
- 运行了哪些测试。
- 还有哪些待核实事项或已知风险。

## 9. 测试要求

正式实现至少覆盖：

- 同一房间重叠日期的并发提交。
- `[checkIn, checkOut)` 边界。
- 已确认、已入住、已完成、已取消和未到店的状态转换。
- 续住、提前退房和换房后的房态与房价。
- 入住和退房的权限与例外审批。
- 押金、多笔收款、退款、挂账和余额。
- 班次差异确认与营业日关闭。
- 普通前台无法调用经理接口。
- 服务重启后的数据一致性。
- 备份恢复到隔离环境。
- 敏感字段在数据库和备份中不可直接读取。

不要使用真实宾客数据做测试。

## 10. 当前命令

历史演示 MVP：

```powershell
node server.mjs
```

原型文件：

```text
prototype/hotel-prototype.html
```

这些命令只用于查看现有产物，不代表正式产品的启动方式。正式框架、构建和测试命令应在开发启动后统一加入项目根目录。

## 11. 禁止事项

- 不要把原型或演示 MVP 当作生产代码继续堆功能。
- 不要使用 JSON 文件保存正式生产业务数据。
- 不要用浮点数计算金额。
- 不要用前端按钮隐藏代替后端权限。
- 不要把房态合并成单一状态字段。
- 不要物理删除订单、账务、退款或审计记录。
- 不要在没有数据库保护的情况下只做前端日期冲突校验。
- 不要添加 PRD 中明确排除的模块。
- 不要在未确认的情况下把 PRD 中的“待核实”改成“已确认”。
- 不要提交真实宾客信息、密钥、密码或生产备份。
